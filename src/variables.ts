// global variables --------------------

import { WindowSize } from './models';

// debug
export const debugSelectedNodes: boolean = false;
export const debugSteps: boolean = false;
export const debugTime: boolean = false;

export const layerNamePrefix: string = '#';

// global variables: window size
export const appWindowMinSize: WindowSize = { w: 380, h: 494 }
export const appWindowMinSizeAdvancedMode: WindowSize = { w: 380, h: 605 }
export const appWindowBigStartSize: WindowSize = { w: 750, h: 650 }
